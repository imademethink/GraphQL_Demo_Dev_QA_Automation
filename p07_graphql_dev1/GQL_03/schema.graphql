type Supplier {
    id: ID!
    companyName: String!
    location: String!
}

type Product {
    id: ID!
    name: String!
    price: Float!
    # Added limit argument (Int)
    suppliers(limit: Int, location: String): [Supplier!]!
}

type Query {
    product(id: ID!): Product
}